<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chat App - Users</title>
    <link rel="stylesheet" href="/styles/main.css">
</head>
<body>
<div class="container">
    <h1>Welcome, <%= currentUser %>!</h1>
    <h2>Available Users</h2>
    <ul class="user-list">
        <% users.forEach(user => { %>
            <li class="user">
                <span><%= user.email %></span>
                <a href="/chat?user=<%= currentUser %>&to=<%= user.email %>" class="chat-icon">💬</a>
            </li>
        <% }) %>
    </ul>
</div>
</body>
</html>